version: "3.8"
services:

  goservices:
    build: "."
    container_name: goservices
    image: naufalhakm/library-api-category:v1
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: user
      DB_PASSWORD: password
      DB_DATABASE: library
    ports:
      - "8084:8084"  # REST API
    restart: always
